import React from 'react'
import ReactDOM from 'react-dom/client'
import FischMinimalDashboard from './FischMinimalDashboard'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FischMinimalDashboard />
  </React.StrictMode>,
)
